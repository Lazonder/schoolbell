#Python
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Logs & runtime
events.jsonl
events.jsonl.*
*.log

# Temp files
*.tmp
*.bak
*.swp

# Sound files (local)
static/geluiden/*.mp3
static/geluiden/*.wav

# Config (echte data) — zie config.example.json voor een template
config.json
roosters.json
dagplanning.json
standaardweek.json
weken_uit.json
vakanties.json
vakanties_fetch_state.json
daemon_heartbeat.json

# Lock files for the JSON state above (created by fcntl.flock on
# the sidecar files in webinterface.locked_json / settings_store.locked).
*.lock

# Secrets
.env
*.env

# OS
.DS_Store

