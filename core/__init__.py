# The "core" package holds small helper modules.
# Each file groups functions that belong together (dates, audio, ...).
# Importing this folder as a package lets you write:
#     from core.dates import iso_week_key
# instead of having one giant webinterface.py file.
